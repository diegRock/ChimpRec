# Requirements for the Final Pipeline notebook (pipeline_final.ipynb).
#
# Install with:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu126
#
# Pinned to match the cluster environment (ly-w111) that produced HOTA=0.6606:
#   Python 3.10.8, torch 2.7.0+cu126, CUDA 12.6, cuDNN 9.5.1
#
# System dependency (not pip-installable):
#   ffmpeg  — required for audio muxing in mux_audio()
#             Install via: sudo apt install ffmpeg  (Debian/Ubuntu)
#                      or: brew install ffmpeg      (macOS)

# --- PyTorch (CUDA 12.6) — install via:
#   pip install torch==2.7.0+cu126 torchvision==0.22.0+cu126 --index-url https://download.pytorch.org/whl/cu126
torch==2.7.0+cu126
torchvision==0.22.0+cu126

# --- Computer vision ---
opencv-python==4.13.0.92
Pillow==12.2.0

# --- Object detection / tracking ---
# Provides YOLO body detector and built-in BYTETracker
ultralytics==8.3.146

# Re-ID backbone used by ui_lib_bytetrack (DeepSort appearance features)
torchreid>=0.2.5

# --- Utilities ---
numpy==2.2.6
tqdm==4.67.3
scipy==1.15.3
lap==0.5.13
